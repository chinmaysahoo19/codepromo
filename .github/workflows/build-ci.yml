name: CI Node JS
on:
  push:
    branches:
      - main
      - release/**
  create:


    tags:
      - v*
jobs:
  build:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
          echo "Helo Helo"